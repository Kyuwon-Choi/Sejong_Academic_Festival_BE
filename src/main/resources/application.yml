spring:
  datasource:
      url: jdbc:h2:tcp://localhost/~/sejong-festival
      username: sa
      password:
      driver-class-name: org.h2.Driver
  jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
  #        show_sql: true
          format_sql: true
  logging.level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

flask:
  server:
    url: http://localhost:5001
